{% extends "::layout.html.twig" %}

{% block content %}
    {% if success is defined %}
        <div class="success">
            {{ success }}
        </div>
    {% endif %}
    {{ form(form) }}
{% endblock %}
