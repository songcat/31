{% extends "ThirtyOneMemberBundle::layout.html.twig" %}

{% block content %}

    {{ render(controller('ThirtyOneMemberBundle:Event:participate', { 'eventId': event.id })) }}

    <img src="{{ asset('uploads/documents/'~event.path) }}" alt=""/>
    {{ event.name }}

{% endblock %}